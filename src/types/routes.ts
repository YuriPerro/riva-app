export enum Route {
  Dashboard = "/",
  Tasks = "/tasks",
  Pipelines = "/pipelines",
  PullRequests = "/pull-requests",
  Settings = "/settings",
  Onboarding = "/onboarding",
  ProjectSelect = "/project-select",
  TeamSelect = "/team-select",
}
