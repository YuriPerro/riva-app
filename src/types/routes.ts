export enum Route {
  Dashboard = '/',
  Tasks = '/tasks',
  Pipelines = '/pipelines',
  PullRequests = '/pull-requests',
  Releases = '/releases',
  Settings = '/settings',
  Onboarding = '/onboarding',
  ProjectSelect = '/project-select',
  TeamSelect = '/team-select',
  Setup = '/setup',
}
